# Local / Expo Go development ONLY (not baked into Play Store builds)
# Phone testing: use your PC LAN IP + port 3000 (website).
# Do NOT use :8081 — that is Expo Metro, not the API.
# Example: http://192.168.1.18:3000
EXPO_PUBLIC_API_URL=http://192.168.1.18:3000
